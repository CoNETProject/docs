---
description: HonKit/GitBook 更新：长 hex 表格不得裁切；必须保留 styles/website.css；发版走 deployGitbook.sh
globs: gitbook/**/*,scripts/deployGitbook.sh,README.md
alwaysApply: false
---

# CoNET GitBook 更新守则

本仓是公开书 **https://gitbook.conet.network/** 的源（HonKit `gitbook/`）。改任何页面或发版时必须遵守本条，防止表格/长 hex 被默认主题裁切。协议双面写回见父仓 `conet-depin-gitbook-sync.mdc`。Beamio 产品机能改动须同步白皮书：父仓 `beamio-gitbook-whitepaper-sync.mdc`（`applications/beamio.md` + `applications/beamio/`）。

## 发版

- 公开正文 **英语**。
- 上线只跑 `./scripts/deployGitbook.sh`（`38.102.126.50` → `/var/www/gitbook.conet.network`）。
- **禁止**为文档新建域名。
- 不要重启 CoNET EL/CL/validator。

## 主题 CSS（禁止删除 / 绕过）

HonKit 默认主题会裁切溢出内容：

```css
.markdown-section { overflow: hidden; }           /* 超出即剪掉 */
.markdown-section table { display: table; overflow: auto; }  /* overflow 在 display:table 上无效 */
```

`table-layout: auto` 下，**无空格的 66 位 hex**（tx / Group ID / bytes32）会把 Value 列撑开，整表宽于 `.markdown-section`（约 770px），右侧列（如 Meaning）被裁成残字。

**唯一修法（已落地，必须保留）：**

| 文件 | 义务 |
|---|---|
| `gitbook/styles/website.css` | 内容区 `overflow-x: auto`；`table-layout: fixed`；`th/td` `overflow-wrap: anywhere`；`td code` / `td a` `word-break: break-all` |
| `gitbook/book.json` | 必须有 `"styles": { "website": "styles/website.css" }` |

**不要**靠缩短、省略或只显示 `0x1234…abcd` 来「修」裁切。标识符表必须显示完整值；折行即可。

## 写表时

- **高风险：** 3+ 列 + 完整 66-hex / 长 URL / 长 `code`。
- 本地 `cd gitbook && npm run build` 后打开 `_book/` 该页：表 `scrollWidth` 不得大于 `.markdown-section` 宽度；右侧列表头与全文须完整。
- 上线后 smoke 该 HTML + `https://gitbook.conet.network/styles/website.css`（须 200），硬刷新核对。

## 严禁

- ❌ 删除、清空或取消挂载 `styles/website.css`
- ❌ 把 `.markdown-section` 改回 `overflow: hidden` 且无横向滚动
- ❌ 用截断 hash / 去掉 Meaning 列当布局修复
- ❌ 手写 `rsync` 覆盖线上却漏掉 `styles/`

## 相关

- 父仓镜像：`.cursor/rules/conet-gitbook-update.mdc`
- 协议双面：`conet-depin-gitbook-sync.mdc`
- 事故页：`developers/l2.html` Identifiers 表（2026-08-17）
