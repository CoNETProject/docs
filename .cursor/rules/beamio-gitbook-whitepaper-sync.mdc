---
description: Beamio 机能改动必须同步 applications/beamio 白皮书
globs: gitbook/applications/beamio.md,gitbook/applications/beamio/**,gitbook/applications/README.md,gitbook/resources.md
alwaysApply: false
---

# Beamio 白皮书（docs 仓镜像）

完整 alwaysApply 守则在父仓 `.cursor/rules/beamio-gitbook-whitepaper-sync.mdc`。

本仓改 **`gitbook/applications/beamio.md`** 或 **`gitbook/applications/beamio/`** 时：

- 公开正文 **英语**。
- 入金双轨不得合并：`walletDeposit` → CONET-USDC；`eoaUsdcStripe` → Base USDC 到 EOA。
- 上线跑 `./scripts/deployGitbook.sh`，保留 `gitbook/styles/website.css`。
- 不要为白皮书新建域名。
