{
  "name": "conet-gitbook",
  "version": "2.0.0",
  "private": true,
  "description": "Self-hosted CoNET GitBook (HonKit) — Applications / L0 / L1 / L2 / Developers",
  "scripts": {
    "build": "honkit build",
    "serve": "honkit serve"
  },
  "devDependencies": {
    "honkit": "^6.0.2"
  }
}
